
import React from "react";

// Optionally import config/utilities if needed in the component
// import { frontendConfig, backendConfig, ... } from "./appConfig";

function App() {
  return (
    <div>
      <h1>Welcome to the Authentication App</h1>
      {/* You can add routing or children here */}
    </div>
  );
}

export default App;
